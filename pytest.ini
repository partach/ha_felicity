[pytest]
testpaths = tests

# The async entity tests are marked with @pytest.mark.asyncio, so strict mode
# (the default) is correct — it keeps an unmarked `async def test_...` an error
# rather than silently never running it.
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
